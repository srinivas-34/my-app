import { OrderspurePipe } from './orderspure.pipe';

describe('OrderspurePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderspurePipe();
    expect(pipe).toBeTruthy();
  });
});
